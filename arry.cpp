//write a program to find sum of n arry element
//roll no-ME21D035
#include<stdio.h>
int main()
{
	int nbr,i,r,arr[10];
	printf("Enter the number of elements in the array:");
	scanf("%d",&nbr);
	printf("Enter the array elements:");
	for (i=0;i<nbr;i++)
	printf("Enter the item to be searched: ");
	scanf("%d",&r);
	if (i<nbr);
	else
	return 0;
}
